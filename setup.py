from setuptools import setup, find_packages

setup(
    name="kaggle_utilities",
    packages=find_packages(),
    version="1.1.0",
    author="Soule",
    install_requires=[
        "GitPython",
    ]
)
